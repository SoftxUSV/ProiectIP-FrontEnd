import { Box } from "@mui/material";
import NavBar from "../Components/NavBar/navbar";

const Layout = () => {
  return (
    <div>
        <Box>
        </Box>
      <NavBar/>
    </div>
  );
};

export default Layout;
